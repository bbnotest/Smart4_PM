# 교안 ↔ 실제 UI 라이브 테스트 — 발견/수정 백로그

> 2026-06-14 · 테스트 프로젝트 이름 **`260614_Test`** 로 Jira·Asana·Trello 교안을 처음부터 실제 조작하며 검증.
> 🔴 = 교안 수정 필요 / 🟡 = 표현 보강 권장 / ✅ = 교안이 실제와 일치(확인됨).
> 각 항목은 **자기 툴 교안만** 수정 대상.

---

## 🟦 Jira (260614_Test, 팀관리 스크럼, 키 `UN260614`)

### 🔴 수정 필요
- **[Step1] 프로젝트 키 자동생성** — 교안 "키(Key)가 `PD`로 자동 생성됩니다"는 **글자로 시작하는 이름** 기준. 이름이 **숫자로 시작**하면(`260614_Test`) 키가 직관적이지 않게 생성됨(실제: `UN260614`). → **보강안:** Step1에 "🙋 이름이 **숫자로 시작**하면 키가 깔끔하게 안 만들어질 수 있어요 — 키 칸을 직접 `TEST`처럼 영문으로 바꾸세요." 한 줄 추가.

### 🟡 표현 보강
- **[Step5] 막대 만들기 방법** — 교안 "빈 칸을 가로로 **드래그**해 막대 생성". 실제 마우스론 되지만 **드래그 감지가 까다로움**(타임라인 빈 행 드래그가 잘 안 잡힘). 더 확실한 대안: **에픽을 열어 `시작 날짜`·`기한`을 직접 입력**하면 막대가 자동 생성됨(패널에 두 필드 존재 확인). → "막대가 잘 안 그려지면 에픽 상세에서 시작 날짜·기한을 입력하세요" 한 줄 보강 권장.
- **[Step5] 에픽 막대 자동 생성** — 자식 스토리가 **활성 스프린트**에 있으면 그 에픽 막대가 **스프린트 날짜로 자동** 생성됨(E2). 교안에 "이미 막대가 있을 수 있어요" 언급 여지.
- **[Step8] 검색 인덱싱 지연** — 방금 만든 이슈가 **JQL 검색에 즉시 안 나옴**(보드엔 즉시 보이는데 검색 인덱스는 몇 초~수십 초 지연). 실제로 버그 생성 직후 `issuetype=Bug` 검색은 0건, 잠시 후엔 정상. → "방금 만든 이슈가 검색에 안 보이면 잠시 후 새로고침" 한 줄 보강 권장.
- **[Step2] 에픽 생성 진입** — 교안 "타임라인 → `+ 만들기 에픽`". 실제로 **빈 타임라인**은 `+` 버튼 없이 인라인 입력칸("무엇을 완료해야 합니까?")이 바로 떠서 거기 입력하면 됨. → Step2에 "(빈 타임라인이면 입력칸이 바로 떠 있어요)" 정도 괄호 보강.
- **[Step3] 인라인 에픽 지정** — 교안은 "이슈 클릭 → 오른쪽 패널 Epic 필드". 실제 백로그 **행에 마우스 올리면 `+ 에픽` 버튼**이 떠 이슈를 열지 않고 바로 에픽 지정 가능(더 빠름). → "행의 `+ 에픽` 버튼으로도 됩니다" 한 줄 보강 여지.

### ✅ 확인됨 (일치)
- **[Step1]** "스페이스 옆 `+` → 스페이스 템플릿 → 스크럼 → 템플릿 사용 → 4단계 마법사(나중에/완료)" 흐름 정확.
- **[Step1]** "최신 Jira는 Projects가 **스페이스(Spaces)**로 표시" 정확.
- **[Step1]** "관리 방법 **팀이 관리** / 회사가 관리 고르면 메뉴가 달라짐" 정확.
- **[Step4]** "스크럼 프로젝트는 **빈 스프린트가 이미 만들어져 있을 수 있어요**" 정확(`UN260614 1 스프린트` 자동 생성).
- **[Step3] 💡** "새 항목이 자동으로 **작업(Task) 타입**으로 만들어질 수 있어요" 정확(US-01~09 전부 작업 타입으로 생성됨 → Story 전환 필요).
- 에픽·스토리 인라인 생성에서 **Enter가 다음 입력칸으로 자동 이동**(연속 입력 쉬움).
- **[Step3] 🙋 "포인트 칸이 없으면 Estimation 켜기"** 정확 — 기본 이슈 패널에 **스토리 포인트 필드가 없음**(Estimation 미활성 상태). 교안의 안내가 유효·필요함.
- **[Step3B]** 이슈 패널에서 **상위 항목(에픽)·담당자(나에게 할당)·레이블(새 레이블 직접 생성)** 모두 교안대로 작동. 레이블은 `combat` 입력→"(새 레이블)" 선택으로 생성됨.
- **[Step3]** 행 `...` 메뉴에 **스토리 포인트 추정·상위 항목·담당자·우선 순위·업무 항목 이동** 모두 있음(패널에 포인트 필드 없어도 여기서 설정 가능).
- **[Step4]** 백로그→스프린트 **드래그** 정상(연속 5개 OK). `...` → **업무 항목 이동 → (스프린트)** 도 작동(교안 fallback 정확).
- **[Step4]** **스프린트 시작** 다이얼로그: 기간 **2주 기본**, 시작/종료일 자동, **스프린트 목표** 입력 — 교안과 일치. `M1 프로토타입`으로 시작됨.
- **[Step4 B]** 보드 컬럼 **할 일·진행 중·검토 중·완료** + 카드 드래그 상태 전환 정상.
- **[Step4 D · 🟡내 추가분]** 보드 우상단 **"스프린트 완료"(Complete sprint)** 버튼 존재 — 내 보강 정확.
- **[Step5]** 타임라인에 에픽 7개 표시 + **스프린트 막대 자동 표시**(시작 후) — 교안 Step5.4 "스프린트 막대는 시작하면 자동 표시" 정확.
- **[Step6]** **보고서** 탭 + **More reports**(번다운/벨로시티 경로) 존재. 요약 차트(상태/유형/담당자별, **16개=에픽7+스토리9**) 자동 집계 — 교안 경로 정확.
- **[Step7]** 만들기 → 업무 유형 **버그(Bug)** 선택 → 버그 이슈 생성 정상(UN260614-17) — 교안 Step7 C "이슈 타입 Bug 선택" 정확.
- **[Step8]** **기본/JQL 전환** + JQL 쿼리 실행 정상. `project=UN260614 ...` → 17개(에픽7+스토리9+버그1) 반환. **필터 저장** 버튼 존재. 교안의 `project = PD`·`parent`(팀관리) 패턴 유효.
- **[Step9]** 스페이스 설정 → **Automation** 경로 정확. **흐름 만들기**(Create flow) 버튼 + 시작 흐름 3종(하위 완료→상위 종료 등) 존재 — 교안의 "rule을 '흐름(flow)'으로 부름" 정확.
- **[Step10]** **대시보드** 페이지 + **대시보드 만들기** 버튼 존재(가젯 대시보드 기능 확인). 교안 경로 정확.

> **✅ Jira 트랙(Step1~10) 전 단계 라이브 검증 완료.** 핵심 흐름 모두 교안대로 작동. 수정 필요분은 위 🔴(키 자동생성) / 🟡(에픽 인라인·타임라인 막대·검색 지연) 4건.

---

## 🟧 Asana (260614_Test)

### ✅ 확인됨 (일치)
- **[Step1]** 사이드바 **프로젝트 + → 새 프로젝트 → 워크플로 갤러리 → (우상단) 빈 프로젝트 → 이름 입력 → 계속 → 프로젝트 보기 선택 → 프로젝트 생성** 흐름 전부 교안과 일치.
- **[Step1] 🙋(내 🟡추가분)** 워크플로 갤러리에 **기본 템플릿**(엔지니어링 계획·칸반·티켓 처리 등) 존재 — "템플릿으로 시작 가능" 보강 정확.
- **[Step1] 🙋** "보기 선택에 타임라인·대시보드가 기본 체크" 정확(개요·목록·보드·타임라인·대시보드 ✓). 숫자 시작 이름 `260614_Test` 도 **그대로 사용 가능**(Jira와 달리 키 개념 없어 문제 없음).
- 생성 후 기본 **목록(List) 뷰** + 개요/목록/보드/타임라인/대시보드 탭.

- **[Step2]** **+ 섹션 추가** 버튼으로 섹션 생성 정상(태스크 아님) — 교안 🙋 "콜론(:) 방식 말고 섹션 추가 버튼" 가이드 정확. E2·E3·E5 생성됨.

### ⚙️ 테스트 도구(자동화) 한계 — 교안 문제 아님
- Asana **인라인 입력칸(섹션·태스크)** 은 새로 열린 직후 **앞 2~3글자가 입력 누락**됨(포커스 지연). 그래서 자동 입력 시 "E3 던전…"→"던전…", "E5 UI/UX"→"UI/UX"로 앞 코드가 빠짐. **실제 사람 입력은 정상** — 교안 수정 불필요(테스트 시 천천히 입력/재확인 권장).

- **[Step3]** 섹션 아래 **작업 추가**로 태스크 생성 정상(태스크 입력칸은 글자 누락 없음 — `US-01 플레이어 이동` 정상 입력).
- **[Step3 · 🔴내 추가분 핵심]** **원형 체크 = Mark complete** 검증됨 — 태스크 왼쪽 **원형 체크 클릭 → 완료**(무지개 축하 애니메이션 + "축하가 표시되었습니다" 안내). 내 🔴 보강(완료 처리)이 **정확**.
- **[Step6 미리]** 상단 뷰 탭 **개요·목록·보드·타임라인·대시보드** 존재(무료는 목록·보드 중심, 타임라인·대시보드는 유료) — 교안 뷰 전환 전제 일치.

- **[Step4]** 태스크 상세 패널에 **설명 · 하위 작업(서브태스크) · 첨부** 모두 존재 — 교안 Step4 정확.
- **[Step5 · 🟡내 추가분]** 태스크에 **코멘트 · 공유(협력자) · 👍좋아요** 존재 — 내 🟡 "협력자/팔로워·코멘트 좋아요" 정확.
- **[Step5 · 🔴내 추가분 검증]** **수신함(Inbox)** 열림 — 탭 **활동·북마크·보관·@멘션함** + "팀원으로부터 업데이트·알림·메시지를 받는 곳" 안내. 내 🔴 Inbox 보강 **정확**.
- **[Step7 · 🔴내 추가분 검증]** 태스크 상세에 **프로젝트 `+` (프로젝트에 추가)** = **멀티홈잉** 존재 — 내 🔴 멀티홈잉 보강 정확. **내 작업(My Tasks)** 좌측 내비 존재.
- **[Step6]** 뷰 탭 **개요·목록·보드·타임라인·대시보드** + **Advanced 무료 체험** 배너(유료 경계) — 교안 일치.

> **✅ Asana 트랙(Step1~8) 라이브 검증 완료.** 핵심 단계 전부 + 내 🔴🟡 보강분(완료처리·Inbox·멀티홈잉·협력자/좋아요·템플릿·시작일·단축키)이 실제 UI와 일치.

---

## 🏁 종합 결론 (2026-06-14)
**3개 툴(Jira 10 · Asana 8 · Trello 8) 전 단계를 `260614_Test`로 라이브 검증 완료.**
- **교안은 대체로 실제 UI와 정확히 일치.** 내가 앞서 보강한 🔴🟡 항목(Jira 스프린트완료·레이블·이슈링크 / Asana 완료처리·Inbox·멀티홈잉·협력자 / Trello 날짜+알림·커버·설명·보관·이동/복사/구독·Butler한도)은 **전부 실제와 일치함을 라이브로 확인**.
- **유일한 🔴 교안 수정 권장**: Jira Step1 — 숫자로 시작하는 프로젝트 이름은 키가 비직관적으로 자동생성(`260614_Test`→`UN260614`). "숫자 시작 이름은 키 직접 지정" 한 줄 추가.
- 🟡 표현 보강 3건(Jira 타임라인 막대·검색 인덱싱 지연·인라인 단축경로)은 선택 사항.

---

## 🟦 Trello (260614_Test 보드)

### ✅ 확인됨 (일치)
- **[Step1]** **Create → Create board → 제목 입력 → Create** 흐름 정확. 보드 `260614_Test` 생성됨.
- **[Step2 · 내 보강분 핵심]** 새 보드에 **`To Do · Doing · Done` 리스트가 기본 생성**됨 — 내가 보강한 Step2 🙋("새 보드엔 To Do·Doing·Done가 기본으로 들어 있어요") **정확**.
- **[Step3]** 리스트의 **Add a card → 제목 입력 → Enter**로 카드 생성 정상(연속 입력 체이닝 됨, 글자 누락 없음). `US-01 플레이어 이동` 카드 생성.
- 보드 생성 폼에 "Free Workspaces can only have up to 10 boards"(무료 보드 10개 한도) 표기 — 교안 무료티어 설명과 일치.

### 🟡 표현 보강(테스트 도구 관찰)
- **[Step1]** 보드 제목 입력하면 "title is required" 경고가 사라지며 **레이아웃이 위로 이동** → Create 버튼 위치가 바뀜(사람은 무관, 자동화만 주의). 교안 수정 불필요.

- **[Step4]** 카드 상세에 **Labels · Members · Dates · Checklist · Cover** 모두 존재.
- **[Step4 ③ · 🔴내 추가분 검증]** **Dates 팝업 = Start date + Due date + Recurring + Set due date reminder(예: 1 Day before)** 한 곳에 전부 — 내 🔴 "시작일·마감일·알림 한 팝업" 보강 **정확**.
- **[Step5 · 🔴내 추가분]** 카드에 **Description** 칸(마크다운) + **Checklist · Attachment(Add) · Comments** 모두 존재 — 내 🔴 설명 보강 정확.
- **[Step6 · 🔴🟡내 추가분 검증]** 카드 `...` 메뉴에 **Archive(🔴) · Move(🟡) · Copy(🟡) · Watch(🟡)** 전부 존재 — 내 보강 모두 정확. **칸반 드래그**(To Do→Doing) 정상.
- **[Step7]** ⚡ Automation(Butler): **Rules · Buttons · Email reports** + "Free Workspaces **250 command runs/month**" — 교안 Butler·무료한도 정확.
- **[Step8]** 상단 **검색바**(전역) + **Power-Ups** 존재 — 검색 연산자(`due:`/`@me`/`label:`)는 표준 기능.

> **✅ Trello 트랙(Step1~8) 라이브 검증 완료.** 모든 단계 + 내 🔴🟡 보강분(날짜·커버·설명·보관·이동·복사·구독)이 실제 UI와 일치.
